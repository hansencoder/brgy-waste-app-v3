Build a clean, high-density "Report Review / Detail View" page for the "Wastewatch" Administrator Portal using React, Tailwind CSS, and Lucide icons based on the following specifications:

1. Color Palette & Base Tokens:
   - Left Navigation Background: Deep dark forest green (#07281E / #0B3024)
   - Primary Active Accent: Vibrant emerald green (#10B981) and mint tint (#E6F4EA)
   - Page Background: Neutral off-white (#F8FAFC)
   - Cards: White background (#FFFFFF) with rounded-2xl (16px) corners, thin borders (#E2E8F0), and soft shadows.

2. Navigation Sidebar (Fixed Left Column, Dark Green):
   - Brand Header: Shield icon with a leaf inside + "Wastewatch" (white bold) and "ADMIN PORTAL" (muted teal sublabel).
   - Menu Items: Dashboard, Reports (Active state: Glowing emerald green pill background with red circle badge "2"), User Mgmt, GIS Monitor, Schedule, Announcements, Analytics, Settings, Audit Logs, Profile.
   - Profile Footer: Avatar "MS" in a green circle, "Maria Santos" (Administrator), and exit icon "Logout".

3. Page Navigation Header:
   - Top Back Link: Left arrow icon with text "< Back to Reports" (clickable hover state, muted gray/dark text).

4. Main Content Area (Two-Column Asymmetric Layout):
   Layout: `grid grid-cols-1 lg:grid-cols-12 gap-6`

   a) Left Primary Column (`lg:col-span-8` - Main Details & Map):
      
      - Card 1: Report Metadata Overview:
        * Header Bar: Report ID `WR-2026-001` (monospace gray) + Status Badge `Pending` (light yellow pill `#FEF3C7`, dark yellow text `#92400E`).
        * Title: "Illegal Dumping" (h1, bold dark text).
        * 2-Column Grid Data Fields (`grid-cols-2 gap-y-4 gap-x-6`):
          • WASTE CATEGORY: "Illegal Dumping" (Bold)
          • ESTIMATED QUANTITY: "Large" (Bold)
          • WASTE CONDITION: "Accumulating" (Bold)
          • PUROK: "Purok 3" (Bold)
          • DATE SUBMITTED: "Jul 20, 2026" (Bold)
          • REMARKS: "Near covered court" (Bold)

      - Card 2: Report Location Map:
        * Header: "Report Location" (h2, bold).
        * Interactive/Static Map Container:
          • Soft green background container (`bg-emerald-50`) with rounded-xl border.
          • Dotted route line overlay.
          • Center marker: Green location pin with text label "Purok 3".
          • Bottom Right Overlay Tag: White pill badge with red map pin icon + "Barangay Dulong Bayan".

   b) Right Secondary Column (`lg:col-span-4` - Reporter, Evidence & Actions Stack):
      
      - Card 1: Reporter Information:
        * Header: "Reporter Information"
        * User Row: Avatar circle "Jd" (light green `#DCFCE7`, dark green text) + Name "Juan dela Cruz" (Bold) + Subtext "Purok 3".
        * Contact Details Stack:
          • Phone icon + `09171234567`
          • Mail icon + `juan@email.com`
          • Document/Folder icon + `Total reports: 8`

      - Card 2: Uploaded Evidence:
        * Header: "Uploaded Evidence"
        * Image Preview Container: Soft light gray/green rounded box with a camera icon in center, text "Photo attached", and subtext "Jul 20, 2026".

      - Card 3: Action Panel (Take Action):
        * Header: "Take Action" (h3, bold).
        * Action Buttons Stack (Full width, rounded-full pills):
          1. Primary Action: Vibrant green button (`#10B981`) with checkmark circle icon + text "Verify Report" (white bold text).
          2. Secondary Action: Light red button (`#FEE2E2` background, `#DC2626` text) with cross circle icon + text "Reject Report".

5. Responsive Design Rules:
   - Mobile (< 640px):
     * Navigation sidebar converts to a slide-over mobile drawer.
     * Left and Right columns stack vertically into a single 1-column layout.
     * Metadata grid shifts to 1 column for small screens (`grid-cols-1`).
     * "Take Action" buttons become sticky at the bottom of the viewport for easy thumb access.
   - Tablet (640px - 1024px):
     * Navigation sidebar collapses to an icon-only 64px vertical rail.
     * Metadata grid remains 2 columns (`grid-cols-2`).
     * Reporter and Take Action cards render side-by-side or stacked cleanly beneath the map.
   - Desktop (> 1024px):
     * Fixed full sidebar (260px) + 12-column grid layout (8 cols left / 4 cols right).