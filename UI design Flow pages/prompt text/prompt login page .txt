Build a clean, high-precision split-screen "Sign In / Login Page" for "Wastewatch" using React, Tailwind CSS, and Lucide icons based on the following specifications:

1. Canvas & Split Layout Architecture:
   - Wrapper: `min-h-screen w-full grid grid-cols-1 lg:grid-cols-12 bg-[#F8FAFC]`
   - Left Column (Hero/Branding): `lg:col-span-5 xl:col-span-5 bg-[#07281E] text-white p-8 lg:p-12 flex flex-col justify-between hidden lg:flex relative overflow-hidden`
   - Right Column (Form Panel): `lg:col-span-7 xl:col-span-7 flex flex-col justify-center items-center p-6 sm:p-12 bg-[#F8FAFC]`

2. Left Branding & Social Proof Column (Dark Forest Green `#07281E`):
   - Brand Header:
     - Logo: Green recycle/shield icon + "Wastewatch" (white bold) + "SMART WASTE SOLUTIONS" (muted teal uppercase sublabel).
     - Badge: Light green pill containing a pulsing green dot + "Live platform · 2026".
   - Headline & Pitch:
     - Heading: "Smarter cities start here." (h1 bold, "start here." rendered in bright teal gradient/mint `#10B981`).
     - Subhead: "Join thousands of municipalities and residents tracking, reporting, and reducing waste in real time."
   - Feature Bullet Points (`space-y-3` with green circular icon backgrounds):
     1. Map Pin Icon -> "Real-time waste mapping & heatmaps"
     2. Bell Icon -> "Instant community alert notifications"
     3. Shield Icon -> "Enterprise-grade data security"
     4. Chart/Activity Icon -> "Advanced recovery analytics"
   - Metrics Stats (3 Columns Row):
     • "24k+" (Bold h3) | Active users
     • "98%" (Bold h3) | Uptime
     • "180+" (Bold h3) | Cities served
   - Testimonial Quote Footer (Border top, translucent dark card):
     * Quote: `"Wastewatch cut our city's illegal dumping incidents by 40% in six months."`
     * Author: Avatar "JR" in teal circle + "James Rivera" | "Director of Sanitation, Metro City".

3. Right Form Column (Login Card Container):
   - Form Container Card: `w-full max-w-md bg-white rounded-3xl p-8 shadow-sm border border-slate-200/80`
   
   - Header Toggle Switcher (Segmented Control Pill `#E6F4EA`):
     * Container: Soft gray/mint rounded pill wrapper with two tab triggers:
       • "Sign in" -> Active state: Solid white pill, shadow-sm, dark text.
       • "Create account" -> Inactive state: Muted gray text, transparent background.

   - Titles:
     - Title: "Welcome back" (h2 bold, dark slate `#0F172A`).
     - Subtitle: "Sign in to your account to continue." (muted slate `#64748B`).

   - Form Fields (`space-y-4`):
     1. EMAIL ADDRESS:
        * Label: Uppercase bold gray text (`text-xs font-semibold text-slate-500`).
        * Input: White pill input (`rounded-xl border border-slate-200 px-4 py-3`), placeholder `you@example.com`.
     2. PASSWORD:
        * Label Row: "PASSWORD" (left) | "Forgot password?" link (right, teal text `#0D9488`).
        * Input: Hidden password field (`••••••••`) with right eye toggle icon (`EyeOff`).
     3. ACCOUNT TYPE (Radio Pill Selector - 2 Columns `grid grid-cols-2 gap-3`):
        * Label: "ACCOUNT TYPE"
        * Option 1: "Resident" -> Selected state: Soft green background (`bg-emerald-50/50`), emerald border (`border-emerald-500`), checked green radio dot.
        * Option 2: "Non-Resident" -> Unselected state: Slate border (`border-slate-200`), gray radio circle.
     4. PUROK:
        * Label: "PUROK"
        * Input: White pill input, uppercase placeholder `PUROK 123`.
     5. VERIFICATION:
        * Box: Simulated reCAPTCHA card container with checkbox, label "I'm not a robot", and reCAPTCHA logo icon.
     6. SUBMIT BUTTON:
        * Primary Action: Full-width dark forest green pill button (`bg-[#07281E] hover:bg-[#051E17] text-white py-3.5 rounded-xl font-medium flex items-center justify-center gap-2`).
        * Text: `-> Sign in` (with right arrow icon).

   - Form Footer:
     * Link Text: "Don't have an account? Sign up free" (Teal `#0D9488` bold link).

4. Responsive Design Rules:
   - Mobile (< 1024px):
     * Left column hides (`hidden lg:flex`).
     * Form panel occupies 100% width, centered on screen with padding.
   - Desktop (>= 1024px):
     * Full 2-column split viewport height (`h-screen`).